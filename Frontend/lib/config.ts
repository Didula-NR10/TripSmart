/**
 * lib/config.ts — app-wide constants that used to live in .env.
 *
 * No more environment variables: edit this file directly to point the app at
 * a different backend.
 *
 * Points at the hosted Render backend — works from anywhere (phone on any
 * network, web/browser testing), not just the same LAN as the dev machine.
 */
export const API_BASE_URL = 'https://tripsmart-ukxh.onrender.com';